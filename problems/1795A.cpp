#include <iostream>

using namespace std;


int main()
{
    int t;
    cin >> t;
    while(t>0)
    {
        t--;

        int n, m;
        cin >> n >> m;

        char s[n];

        for (int i=0; i<n; i++)
        {
            cin >> s[i];
        }

        char t[m];
        for (int j=0; j<m; j++)
        {
            cin >> t[j];
        }

    }

    return 0;
}
